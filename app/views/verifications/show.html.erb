<div class="max-w-md mx-auto">
  <div class="bg-blue-900 text-white py-8 sm:py-12 px-4 sm:px-6 mb-8">
    <div class="text-center">
      <h1 class="text-3xl font-bold mb-4">Verify Visa Letter</h1>
      <p class="text-lg text-gray-300">Enter the verification code from your visa invitation letter to confirm its authenticity.</p>
    </div>
  </div>

  <div class="bg-white border border-gray-300 shadow-sm p-4 sm:p-8">
    <%= form_with url: verify_path, method: :post, class: "space-y-6", data: { turbo: false } do %>
      <div>
        <%= label_tag :code, "Verification Code", class: "block text-sm font-semibold text-gray-700 mb-1" %>
        <%= text_field_tag :code, @verification_code,
            class: "w-full border border-gray-300 shadow-sm p-3 text-center font-mono tracking-wider focus:ring-blue-500 focus:border-blue-500",
            placeholder: "Enter 32-character code",
            autofocus: true %>
      </div>

      <div class="pt-4">
        <%= submit_tag "Verify Letter →", class: "w-full bg-blue-700 text-white py-3 px-4 font-semibold hover:bg-blue-900 transition-colors cursor-pointer" %>
      </div>
    <% end %>

    <div class="mt-8 pt-6 border-t border-gray-200 text-center text-sm text-gray-500">
      <p>This verification system confirms that a visa invitation letter was officially issued by The Hack Foundation.</p>
    </div>
  </div>
</div>
